FolderToAppCacheManifest
========================

Outputs all files in a folder as text ready to be put in a app cache manifest.

This is very useful for things like Images you want to add to a web cache.
